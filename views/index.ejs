<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Log In</title>
</head>
<body>
    <%- include('partials/navbar')%>
    <% messages.forEach((message) => { %>
        <%- include('categoryPages/categoryOptionsNew', {category}); %>
    <% }); %>
</body>
</html>
