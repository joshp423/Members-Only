<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Log In</title>
</head>
<body>
    <%- include('partials/navbar') %>
    <% if (locals.user) {%>
        <div class="titleLinks">
            <h1>Welcome Back <%= user.firstname %></h1>
            <a href="/new-message"><button>Write New Message</button></a>
            <a href="/membership-gateway"><button>Membership gateway</button></a>
            <a href="/log-out"><button>Log Out</button></a>
        </div>
    <% } else { %>
        <h1>Log in to create messages, members can view who sent the messages.</h1>
        <form action="/log-in" method="POST">
        <label for="username">Email</label>
        <input id="username" name="username" placeholder="username" type="email" />
        <label for="password">Password</label>
        <input id="password" name="password" type="password" />
        <button type="submit">Log In</button>
        </form>
    <%}%>
    <%- include('messages/messageBoard', {messages, user}) %>
</body>
</html>
