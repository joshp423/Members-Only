<nav>
  <h1>Members Only</h1>
  <ul>
      <li>
        <a href="/">
          <span></span>
        </a>
        <a href="/authenticate"></a>
          <% if (locals.user) {%>
            <span>Log Out</span>
          <% } else { %>
            <span>Log In</span>
          <%}%>
        </a>
      <% if (locals.user) {%>
        <a href="/sign-up">
          <span>Sign Up</span>
        </a>
      <% } %>
      </li>
  </ul>
</nav>